请解析以下旅行查询，输出 QueryUnderstandingResult JSON。

raw_user_query:
{{raw_user_query}}

conversation_context:
{{conversation_context}}

supported_regions:
{{supported_regions}}

known_place_hints:
{{known_place_hints}}

current_date:
{{current_date}}

只输出 JSON，不要其他文字。
