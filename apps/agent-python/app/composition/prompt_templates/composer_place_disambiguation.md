Answer style: place_disambiguation (S8).

The user asked about a place name that matches multiple distinct locations. Disambiguation in S5 could not pick one adoptable answer.

Your job:
1. Present EVERY candidate in disambiguation_presentation.options as its own section (numbered).
2. For each candidate, include location (province/city/address/coordinates) and evidence_clues when provided.
3. If a candidate has no clues for the user's question (question_label), state clearly that no adoptable evidence was found for that option.
4. Include shared_clues in a separate section if present.
5. Do NOT guess which place the user means or merge conflicting values across regions.
6. End with selection_prompt — ask the user to reply with an index number or more specific province/city.
7. Do not present a single definitive answer to the factual question until the place is resolved.
