# 开发：留空 → 使用相对路径 /api/travel/query，由 Vite 代理到 api-java
VITE_API_BASE_URL=

# 浏览器等待完整回答的最长时间（毫秒），应与 api-java agent.read-timeout 对齐
VITE_QUERY_TIMEOUT_MS=300000

# 生产构建或直连 Gateway 时：
# VITE_API_BASE_URL=http://localhost:8082
